<%= form_with(model: entry) do |form| %>
  <% if entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
        <% entry.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :country %>
    <%= form.text_field :country %>
  </div>

  <div class="field">
    <%= form.label :population_group %>
    <%= form.text_field :population_group %>
  </div>

  <div class="field">
    <%= form.label :food_group %>
    <%= form.text_field :food_group %>
  </div>

  <div class="field">
    <%= form.label :subjects %>
    <%= form.number_field :subjects %>
  </div>

  <div class="field">
    <%= form.label :consumers %>
    <%= form.number_field :consumers %>
  </div>

  <div class="field">
    <%= form.label :mean %>
    <%= form.text_field :mean %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
