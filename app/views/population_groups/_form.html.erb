<%= form_with(model: population_group) do |form| %>
  <% if population_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(population_group.errors.count, "error") %> prohibited this population_group from being saved:</h2>

      <ul>
        <% population_group.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
